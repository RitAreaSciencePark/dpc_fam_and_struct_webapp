apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: dpcexplorer-postgresql
spec:
  instances: 1

  bootstrap:
    initdb:
      database: dpcexplorer
      owner: dpcexplorer_app

  storage:
    size: 50Gi
