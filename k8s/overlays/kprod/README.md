# kprod overlay

The production overlay will be created during Milestone 5 after the kprod
namespace, storage classes, ingress class, resource quota, and backup policy
are confirmed.

Do not copy the kdevel overlay into production without verifying those values.