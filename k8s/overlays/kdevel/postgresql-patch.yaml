apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: dpcexplorer-postgresql
spec:
  storage:
    storageClass: rbd-nvme-replica

  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: "1"
      memory: 2Gi
