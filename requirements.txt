# Web framework
Django==6.0.1

# PostgreSQL adapter for Python
psycopg2-binary==2.9.11

# Search/filter forms for querysets
django-filter==25.2

# Paginated, sortable HTML tables from querysets
django-tables2==2.8.0

# Production WSGI server
gunicorn==26.0.0

# Serves static files directly from Django
whitenoise==6.11.0

# Reads secrets from .env file
python-decouple==3.8

# Internal Django and DB utilities
asgiref==3.11.0
sqlparse==0.5.3